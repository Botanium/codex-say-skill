# Python regular expressions removed before Markdown cleanup and speech.
# Blank lines and lines starting with # are ignored.

# Codex memory citation payloads and the collapsed visible citation row.
(?is)<oai-mem-citation\b[^>]*>.*?</oai-mem-citation>
(?im)^[^\S\n]*<?[^\S\n]*\d+[^\S\n]+memory citations?>?[^\S\n]*$

# GitHub/Git commit hash noise in final status summaries.
(?im)^[^\S\n]*(?:pushed commit|commit|github commit|pushed to github|latest commit)[^\S\n]*:?[^\S\n]*`?[0-9a-f]{7,40}`?\.?[^\S\n]*$
(?i)\b[0-9a-f]{40}\b
